name: RealtyPlanExtension
version: "${version}"
main: io.github.md5sha256.realty.plan.RealtyPaperPlanExtensionPlugin
api-version: "26.1.2"
authors: [ md5sha256 ]
dependencies:
  server:
    Realty:
      load: BEFORE
      join-classpath: true
      required: true
    Plan:
      load: BEFORE
      join-classpath: true
      required: true
